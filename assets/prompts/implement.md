# TASK

Implement GitHub issue {{TASK_ID}}: {{ISSUE_TITLE}}

Read it with `gh issue view {{TASK_ID}} --comments`. Work only on this issue and
only on branch `{{BRANCH}}`.

Inspect the repository instructions, implementation, and relevant tests before
editing. Implement the smallest complete change, add appropriate tests, run the
repository's documented verification commands, and commit the result.

Do not close the issue. The merge stage closes successfully merged issues.
If blocked, comment on the issue and make no partial commit.

When complete, output `<promise>COMPLETE</promise>`.
